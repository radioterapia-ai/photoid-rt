<?xml version="1.0" encoding="utf-8"?>
<!--
    Onboarding (responsivo, sem barra superior):
    - Conteúdo do passo em ScrollView ocupa toda a área.
    - Contador "Passo X/7" FLUTUA no canto superior direito, em pílula
      semi-transparente para contraste, sem ocupar espaço próprio.
    - Barra inferior: Pular tudo / Anterior / Próximo.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/wizardStepContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:paddingTop="56dp"
                android:paddingBottom="20dp" />
        </ScrollView>

        <TextView
            android:id="@+id/txtWizardPasso"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="12dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:background="@drawable/bg_step_pill"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp"
        android:background="@color/bg_card">

        <Button
            android:id="@+id/btnWizardPular"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="@string/wiz_skip_all"
            android:textSize="11sp"
            android:textColor="@color/text_secondary"
            android:background="?attr/selectableItemBackground" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <Button
            android:id="@+id/btnWizardAnterior"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="@string/back"
            android:textColor="@color/text_on_dark"
            android:backgroundTint="@color/action_neutral"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnWizardProximo"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="6dp"
            android:text="@string/next"
            android:textStyle="bold"
            android:textColor="@color/text_on_dark"
            android:backgroundTint="@color/brand_primary" />
    </LinearLayout>
</LinearLayout>
