<?xml version="1.0" encoding="utf-8"?>
<!--
    Grupo de idiomas em Configurações (item 10).
    
    O modo "auto" foi removido nesta rodada — o app tem 3 idiomas suportados (pt/en/es)
    e o usuário escolhe um. O checkbox chkLangAuto fica oculto (width=0/height=0/gone) 
    apenas pra não quebrar o findViewById no SettingsActivity sem precisar mexer no Kt.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <CheckBox
        android:id="@+id/chkLangAuto"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <TextView
        style="@style/FieldLabel"
        android:text="@string/select_language" />

    <Spinner
        android:id="@+id/spinnerLanguage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp" />

    <!--
        FORMATO DE DATA, junto do idioma porque e a mesma pergunta: como este
        servico escreve as coisas. Vale para o que o tecnico DIGITA; o que e
        gravado continua canonico, e a ficha impressa continua com o mes por
        extenso, que nao e ambiguo em idioma nenhum.
    -->
    <TextView
        style="@style/FieldLabel"
        android:text="@string/date_format_label" />

    <Spinner
        android:id="@+id/spinnerFormatoData"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="13sp"
        android:textColor="@color/text_tertiary"
        android:text="@string/date_format_hint" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:padding="10dp"
        android:textSize="13sp"
        android:textColor="@color/text_secondary"
        android:background="@drawable/bg_card_warning"
        android:text="@string/lang_restart_note" />
</LinearLayout>
