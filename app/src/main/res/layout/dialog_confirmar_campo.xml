<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/bg_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- 1) O campo a confirmar vem PRIMEIRO: textbox branco + visto -->
        <TextView
            android:id="@+id/txtLabelCampo"
            style="@style/FieldLabel" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <EditText
                android:id="@+id/edtCampoConfirmacao"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="46dp"
                android:textSize="16sp"
                android:textColor="#212121"
                android:textColorHint="#9E9E9E"
                android:background="@drawable/bg_input_white"
                android:paddingHorizontal="12dp"
                android:inputType="textCapWords" />

            <ImageButton
                android:id="@+id/btnVistoCampo"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_check_confirm"
                android:background="@drawable/bg_icon_visor"
                android:scaleType="centerInside"
                android:contentDescription="@string/confirm" />
        </LinearLayout>

        <TextView
            android:id="@+id/txtDicaCampo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textColor="@color/text_tertiary"
            android:layout_marginTop="4dp" />

        <!-- 2) Texto bruto detectado (referência, menor) -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/detected_text"
            android:textSize="11sp"
            android:textColor="@color/text_secondary"
            android:layout_marginTop="14dp" />

        <EditText
            android:id="@+id/edtTextoDetectado"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:padding="8dp"
            android:background="@color/bg_card"
            android:textColor="@color/text_secondary"
            android:gravity="top|start"
            android:scrollbars="vertical" />

        <!-- 3) Miniatura da etiqueta (só quando existe imagem) -->
        <ImageView
            android:id="@+id/imgEtiquetaCapturada"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:layout_marginTop="10dp"
            android:scaleType="fitCenter"
            android:background="@color/bg_card"
            android:visibility="gone"
            android:contentDescription="Etiqueta capturada" />
    </LinearLayout>
</ScrollView>
